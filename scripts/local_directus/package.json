{
  "name": "vibetable-local-directus",
  "version": "1.0.0",
  "private": true,
  "description": "Runtime-introduced local Directus 12 server (SQLite) for VibeTable development and smoke testing. Mirrors the form the installer will eventually distribute: the program pulls Directus at runtime via npm, never bundled statically.",
  "type": "module",
  "scripts": {
    "start": "directus start",
    "start:dev": "directus start --watch"
  },
  "dependencies": {
    "directus": "^12.0.0"
  },
  "overrides": {
    "isolated-vm": "6.1.2"
  }
}
