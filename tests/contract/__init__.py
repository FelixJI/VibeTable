"""Language-neutral contract tests.

These tests pin the on-the-wire shape of JSON-RPC method calls so that the
Python backend (Task 3) and the C# WPF client (Task 5) stay in sync against
the same fixture files under ``tests/contract/fixtures``.
"""
