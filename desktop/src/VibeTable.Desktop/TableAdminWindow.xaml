<Window x:Class="VibeTable.Desktop.TableAdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新建表" Height="480" Width="460"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="新建表" FontSize="20" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Margin="0,6,0,14"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   Text="定义表名与字段。创建后即可在表中录入数据。" TextWrapping="Wrap"/>

        <!-- Table name + dynamic field list. Each field row is added in code-behind. -->
        <DockPanel Grid.Row="2" LastChildFill="True">
            <Grid DockPanel.Dock="Top" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="表名" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="NameBox" Grid.Column="1" Height="30" Padding="6,4"/>
            </Grid>
            <Button DockPanel.Dock="Top" Content="+ 添加字段" Click="OnAddField"
                    HorizontalAlignment="Left" Padding="12,4" Margin="0,0,0,6"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FieldsPanel"/>
            </ScrollViewer>
        </DockPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="3" Margin="0,10,0,0"
                   Foreground="#ff8a80" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" Width="80" Height="30" Margin="0,0,8,0" IsCancel="True"/>
            <Button x:Name="CreateButton" Content="创建" Width="88" Height="30"
                    IsDefault="True" Click="OnCreateClick"/>
        </StackPanel>
    </Grid>
</Window>
