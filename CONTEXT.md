# VibeTable

VibeTable 是离线优先的桌面多维表格产品；本上下文定义产品界面、数据契约与内置 PocketBase 基础设施之间的统一语言。

## Language

**VibeTable 仪表盘**:
由用户配置图表、指标和面板的数据展示功能，不直接代表底层数据管理入口。
_Avoid_: Dashboard、PB Dashboard

**PocketBase 管理后台**:
PocketBase 自带的超级管理员后台，仅作为显式的开发者危险入口，不作为产品表数据或 Schema 的常规写入入口。
_Avoid_: Dashboard、VibeTable 仪表盘

**产品写入边界**:
产品表的数据和 Schema 在正常模式下必须经过 VibeTable 契约及其权威写入服务；绕过该边界的操作不享有无损与一致性保证。
_Avoid_: 直接写 PB、后台直改

**空白值**:
单元格没有值的产品状态，在线路契约中表示为 `null`；它与 `0`、`false`、`(0,0)`、空文本及空容器等已提供的值不同。
_Avoid_: PocketBase 零值、默认值、空字符串

**互操作导出**:
面向 CSV、XLSX 等通用格式的数据副本，服务于外部查看和处理；允许按已声明规则把空白值规范化为空单元格，不承诺无损往返。
_Avoid_: 无损导出、备份

**无损备份**:
用于恢复 VibeTable 完整状态的保全通道，必须保留产品 Schema、字段元数据、空白存在性及原始值语义。
_Avoid_: CSV 导出、Excel 导出

**存在性列**:
由 VibeTable 为不能原生保存空白值的可空字段创建的内部布尔伴生列；它与字段值在同一权威事务中写入，用于区分空白值与 PocketBase 零值。
_Avoid_: `__vt_nulls`、按需空值索引、用户字段

**整数**:
在产品声明范围内逐位精确保存的无小数数值。
_Avoid_: Integer、浮点整数

**数字格式**:
叠加在通用数值上的字段级显示元数据，包括整数、小数位、千分位、百分比、货币符号和币种；它不改变已保存的原始值，也不构成十进制精确性或舍入策略承诺。
_Avoid_: 存储精度、精确金额、隐式舍入

**通用数字**:
VibeTable 首版唯一开放的可计算数值值域，采用 IEEE 754 binary64 语义；整数、小数位、千分位、百分比和货币均为其字段格式或预设。输入超出声明的安全边界、发生溢出，或转换后用户可见数值发生变化时必须明确拒绝，不得静默修正。
_Avoid_: 精确金额、Decimal、任意精度数

**精确小数**:
面向对账、计税等明确需求的候选高级能力；首版不开放，也不作为货币格式的隐含保证。若后续立项，必须同时定义十进制表示、运算与舍入规则、自动数据迁移以及导入导出契约。
_Avoid_: Decimal、浮点数、显示精度

**字段数据迁移**:
字段存储口径改变时由 VibeTable 自动执行的扫描、转换、校验、原子切换和失败回滚流程；用户不需要导出、手工修改或重新导入数据。
_Avoid_: 手工改列、静默类型转换

**内容型记录**:
按表级内容配置将结构化记录呈现为可阅读、可编辑正文的产品形态；记录仍是唯一身份，正文不是第二份独立记录。
_Avoid_: Notion 页面、块文档、内容副本

**内容配置**:
数据表对内容型记录的展示约定，指定标题、正文、摘要及进入统一搜索的字段。
_Avoid_: ContentProfile、页面模板、搜索配置

**托管附件**:
从属于某条记录某个附件字段的版本化资源，其生命周期随记录字段历史管理。
_Avoid_: 文件文档、工作区文件、独立文档

**文件文档**:
在工作区中具有独立身份、相对路径和版本历史的文件，不从属于某条记录或某个字段。
_Avoid_: 托管附件、Attachment、文件副本

**记录文档关联**:
结构化记录与文件文档之间可修复、无级联删除的显式关系；删除关联不删除任一端对象。
_Avoid_: 附件字段、隐式绑定、级联附件

**派生搜索索引**:
从结构化记录、托管附件和文件文档生成的可丢弃搜索投影；它不是业务权威，损坏或版本变化时必须从权威数据重建。
_Avoid_: 搜索数据库、全文权威、备份内容

**VibeTable 界面**:
由页面、数据绑定和闭集动作组成的本地业务操作界面，面向记录列表、详情、表单和导航；它与只读分析用途的 VibeTable 仪表盘是不同产品对象。
_Avoid_: Interface、Dashboard、任意应用画布

**行历史恢复**:
把历史业务值恢复到当前字段模型的记录级操作；计算字段重新计算，不承诺恢复历史字段模型。
_Avoid_: 时间旅行、完整快照恢复

**工作区快照恢复**:
把工作区数据、文件状态、后台任务状态和审计锚点恢复到同一受协调时间点的整体操作。
_Avoid_: 行历史恢复、导入、回滚单条记录
